# Skill Registry — ServiceDeskDESI

Generated by sdd-init on 2026-08-17.

## Active Workflow

- **SDD suite (Engram mode)** — `sdd-explore`, `sdd-propose`, `sdd-spec`, `sdd-design`, `sdd-tasks`, `sdd-apply`, `sdd-verify`, `sdd-archive`, `sdd-init`, `sdd-onboard` (user-level, `~/.config/opencode/skills/`). Artifact store: **engram**.
- `customize-opencode` — built-in (opencode config only).

## Project-Level Skills

Located under `.github/skills/` (OpenSpec / GitHub Spec Kit workflow — requires `openspec` CLI).

| Skill | Trigger / Description |
|-------|----------------------|
| `openspec-propose` | Propose a new change; generate proposal, spec delta, design, and tasks in one step. Planning only — never edits code. |
| `openspec-explore` | Enter explore mode: thinking partner for ideas, investigation, clarification. Read-only stance; never implements. |
| `openspec-apply-change` | Implement tasks from an OpenSpec change. |
| `openspec-archive-change` | Archive a completed change. |
| `openspec-sync-specs` | Sync delta specs to main specs without archiving. |
| `openspec-update-change` | Revise existing planning artifacts; keeps them coherent. Never edits code. |

## Project Conventions

No agent instruction files detected (`agents.md`, `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `GEMINI.md`, `copilot-instructions.md`).

## Notes

- Existing `openspec/` directory present (config.yaml boilerplate, empty `specs/` and `changes/`). SDD init did NOT modify it (Engram mode).
- `.github/prompts/opsx-*.prompt.md` (6 files) pair with the openspec skills above.
